model Portfolio {
  id            String      @id @default(uuid(7))

  name          String 
  description   String
  link          String

  createdAt     DateTime    @default(now())

  skills        Skill[]
  experience    Experience[]
  projects      Project[]
}